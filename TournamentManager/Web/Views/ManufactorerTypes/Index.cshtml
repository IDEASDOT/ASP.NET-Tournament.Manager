@model IEnumerable<Domain.ManufactorerType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ManufactorerTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ManufactorerTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ManufactorerTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ManufactorerTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ManufactorerTypeId })
        </td>
    </tr>
}

</table>
