@model IEnumerable<Domain.ModelSerieType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ModelSerieTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ModelSerieTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ModelSerieTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ModelSerieTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ModelSerieTypeId })
        </td>
    </tr>
}

</table>
