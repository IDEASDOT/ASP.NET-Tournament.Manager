@model Web.Areas.Admin.ViewModels.ManufactorerTypeIndexViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink(@Resources.Common.CreateNew, "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ManufactorerTypes.FirstOrDefault().ManufactorerTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.ManufactorerTypes) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ManufactorerTypeName)
        </td>
        <td>
            @Html.ActionLink(@Resources.Common.Edit, "Edit", new { id=item.ManufactorerTypeId }) |
            @Html.ActionLink(@Resources.Common.Details, "Details", new { id=item.ManufactorerTypeId }) |
            @Html.ActionLink(@Resources.Common.Delete, "Delete", new { id=item.ManufactorerTypeId })
        </td>
    </tr>
}

</table>
